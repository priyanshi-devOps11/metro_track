import 'package:flutter/material.dart';

class StationNavigatorScreen extends StatelessWidget {
  const StationNavigatorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Station Navigator - Coming Soon'),
    );
  }
}